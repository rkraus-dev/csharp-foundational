# Anagram Check
A console-based anagram checker built in C#.

## Features
- Checks if two strings are anagrams of each other
- Case-insensitive comparison via ToLower()
- Sorts characters alphabetically and compares

## Example
```
Enter first string: listen
Enter second string: silent
"listen" and "silent" are anagrams!

Enter first string: hello
Enter second string: world
"hello" and "world" are not anagrams!
```

## Chapter
04 - Strings and Methods

## Topics Practiced
- ToCharArray() to convert string to char array
- Array.Sort() for alphabetical sorting
- new string(char[]) to convert back to string
- ToLower() and Trim() for string normalization
- String comparison with ==